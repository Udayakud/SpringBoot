In this Micro service to prevent Hard coded value of port number we use Netflix Eureka naming server so that Any request we can directly get it from the Naming Server

Eureka acts as a client side loadBalancing

